# dsh-plugin

给已经在用 DeepSeek Harness 的团队补协作层。官方 Web 是单人 Creator/调试；这里是多角色同一房间、@ 分派、会话可续，体验对齐 Grok Bot，以 Cordis 插件长在 Harness 里。

MVP：房间会话、成员路由、@ 分派、工具权限拒绝、可回放 session log。钉 `@deepseek-ai/dsh@0.1.1-rc.2`。不做桌面端、完整账号、Gmail/Drive/X。
